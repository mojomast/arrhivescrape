# Production Readiness Review

Production readiness depends on strict static output, validation before promotion, privacy review before exposure, predictable serving behavior, and generated operational config that does not modify system services by default.

Public publication should be explicitly opt-in, should support `noindex` review modes, and should block on forms, sensitive query parameters, unresolved private assets, or missing approval.
